server:
  port: 8080

spring:
  application:
    name: restful-service
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

logging:
  level:
    org.springframework: DEBUG

# Monitoring
management:
  endpoints:
    web:
      exposure:
        include: "*"